# rwdb-vite-client

## Setup

- Install `bun` ( faster alternative of Node.js )

```bash
curl -fsSL https://bun.sh/install | bash
```

- Install dependencies

```bash
bun i
```

## Run

```bash
bun run dev --host
```

